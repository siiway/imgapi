# Agent Notes

- Run `ruff check .` for lint checks.
- Run `ty check .` for Astral type checks.
- Run `prek run --all-files` for pre-commit hook checks.
- Prefer targeted checks for changed files first, then broader checks when practical.
